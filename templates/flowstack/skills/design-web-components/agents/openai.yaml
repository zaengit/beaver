interface:
  display_name: "Web Component Design"
  short_description: "Build CMS web sections and templates"
  default_prompt: "Use $design-web-components to create a new CMS web section in src/components/web. Keep public content data-driven and update authorized data/seed.json only when seed content is explicitly requested."
