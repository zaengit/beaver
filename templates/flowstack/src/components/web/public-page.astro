---
import type { Post } from "@/shared/types/posts"
import PageSections from "@/components/web/page-sections.astro"

interface Props { page: Pick<Post, "sections"> }
const { page } = Astro.props
---

<main>
  <PageSections sections={page.sections} />
</main>
